Copied from `reference/` at the repository root, which is the single source of truth.

Refresh with:

```bash
npm run sync:skill
```

Never edit these files directly. Edit `reference/` and re-sync, so the plugin, the skill, and the
board never disagree about what a Sev 3 is.
